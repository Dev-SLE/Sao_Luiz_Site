/**
 * Cobertura CRM (Fase 4) já presente no workspace: `Page.CRM_*` em `types.ts` + telas em `components/Crm*.tsx`.
 * Lacunas típicas vs `fase_1.md`: Empresas, Motivos de perda, Agenda CRM dedicada — evoluir sob `modules/crm/`.
 */
export const FASE4_CRM_ROUTES = ['/app/crm/dashboard', '/app/crm/funil', '/app/crm/chat', '/app/crm/tarefas'] as const;
