{
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix pendencias-sle-main",
    "build": "npm run build --prefix pendencias-sle-main",
    "start": "npm run start --prefix pendencias-sle-main",
    "vercel-build": "npm install --include=dev --prefix pendencias-sle-main && npm run build --prefix pendencias-sle-main"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "googleapis": "^171.4.0",
    "multer": "^2.1.1",
    "next": "^15.0.0"
  }
}
